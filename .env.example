# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Direct Postgres connection (Drizzle migrations + seed)
# Supabase: Project Settings > Database > Connection string (use the pooler for app, direct for migrations)
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Anthropic (estimate engine — Phase 3)
ANTHROPIC_API_KEY=

# Feature flags
FEATURE_AI_ESTIMATE=false
